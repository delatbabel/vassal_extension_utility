#!/bin/bash
#
# Launcher for the VASSAL Extension Utility .app bundle.
# Runs the bundled JRE against the fat JAR shipped in Resources/Java.
#
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/jre/bin/java" -Xdock:name="VASSAL Extension Utility" \
    -jar "$DIR/../Resources/Java/@JARFILE@" "$@"
